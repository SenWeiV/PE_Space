http:
  routers:
    app-32:
      rule: "PathPrefix(`/apps/dasda`)"
      middlewares:
        - pe-auth
      service: app-32-svc
      priority: 5

  services:
    app-32-svc:
      loadBalancer:
        servers:
          - url: "http://10.104.29.28:8600"
