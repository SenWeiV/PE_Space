http:
  routers:
    app-16:
      rule: "PathPrefix(`/apps/test111`)"
      middlewares:
        - pe-auth
      service: app-16-svc
      priority: 5

  services:
    app-16-svc:
      loadBalancer:
        servers:
          - url: "http://10.104.29.28:8600"
